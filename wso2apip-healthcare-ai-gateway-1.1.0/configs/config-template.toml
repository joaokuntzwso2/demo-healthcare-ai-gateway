# Helios Clinical AI Security - WSO2 AI Gateway 1.1.0 local configuration
# Derived from the WSO2 1.1.0 configuration used by the canonical banking demo.
[controller.server]
api_port = 9090
xds_port = 18000
shutdown_timeout = "15s"
gateway_id = "helios-healthcare-ai-security-gateway"

[controller.admin_server]
enabled = true
port = 9092
allowed_ips = ["*"]

[controller.policy_server]
port = 18001
[controller.policy_server.tls]
enabled = false
cert_file = "./certs/server.crt"
key_file = "./certs/server.key"

[controller.controlplane]
host = ""
token = ""
reconnect_initial = "1s"
reconnect_max = "5m"
polling_interval = "15m"
insecure_skip_verify = false

[controller.storage]
type = "sqlite"
[controller.storage.sqlite]
path = "./data/gateway.db"

[controller.policies]
definitions_path = "./default-policies"

[controller.auth.basic]
enabled = true
[[controller.auth.basic.users]]
username = "admin"
password = "admin"
password_hashed = false
roles = ["admin"]

[controller.auth.idp]
enabled = false
jwks_url = ""
issuer = ""

[api_key]
api_keys_per_user_per_api = 10
algorithm = "sha256"
issuer = "api-platform-devportal"

[controller.logging]
level = "info"
format = "text"
[controller.metrics]
enabled = true
port = 9091

[controller.event_hub]
poll_interval = "3s"
cleanup_interval = "10m"
retention_period = "1h"
[controller.event_hub.database]
max_open_conns = 5
max_idle_conns = 2
conn_max_lifetime = "30m"
conn_max_idle_time = "5m"

[router]
gateway_host = "*"
listener_port = 8080
https_enabled = true
https_port = 8443
tracing_service_name = "router"
[router.access_logs]
enabled = true
format = "text"
text_format = """[%START_TIME%] \"%REQ(:METHOD)% %REQ(X-ENVOY-ORIGINAL-PATH?:PATH)% %PROTOCOL%\" %RESPONSE_CODE% %RESPONSE_FLAGS% %DURATION% \"%REQ(X-REQUEST-ID)%\" \"%UPSTREAM_HOST%\"\n"""

[router.downstream_tls]
cert_path = "./listener-certs/default-listener.crt"
key_path = "./listener-certs/default-listener.key"
minimum_protocol_version = "TLS1_2"
maximum_protocol_version = "TLS1_3"
ciphers = "ECDHE-ECDSA-AES128-GCM-SHA256,ECDHE-RSA-AES128-GCM-SHA256,ECDHE-ECDSA-AES128-SHA,ECDHE-RSA-AES128-SHA,AES128-GCM-SHA256,AES128-SHA,ECDHE-ECDSA-AES256-GCM-SHA384,ECDHE-RSA-AES256-GCM-SHA384,ECDHE-ECDSA-AES256-SHA,ECDHE-RSA-AES256-SHA,AES256-GCM-SHA384,AES256-SHA"

[router.upstream.tls]
minimum_protocol_version = "TLS1_2"
maximum_protocol_version = "TLS1_3"
ciphers = "ECDHE-ECDSA-AES128-GCM-SHA256,ECDHE-RSA-AES128-GCM-SHA256,ECDHE-ECDSA-AES128-SHA,ECDHE-RSA-AES128-SHA,AES128-GCM-SHA256,AES128-SHA,ECDHE-ECDSA-AES256-GCM-SHA384,ECDHE-RSA-AES256-GCM-SHA384,ECDHE-ECDSA-AES256-SHA,ECDHE-RSA-AES256-SHA,AES256-GCM-SHA384,AES256-SHA"
trusted_cert_path = "/etc/ssl/certs/ca-certificates.crt"
custom_certs_path = "./certificates"
verify_host_name = true
disable_ssl_verification = false
[router.upstream.timeouts]
route_timeout_ms = 60000
route_idle_timeout_ms = 300000
connect_timeout_ms = 5000

[router.http_listener]
server_header_transformation = "OVERWRITE"
server_header_value = "WSO2 API Platform"

[router.policy_engine]
host = "policy-engine"
port = 9001
timeout_ms = 60000
failure_mode_allow = false
allow_mode_override = true
message_timeout_ms = 60000
[router.policy_engine.tls]
enabled = false
cert_path = ""
key_path = ""
ca_path = ""
server_name = ""
skip_verify = false

[policy_engine.server]
extproc_port = 9001
[policy_engine.admin]
enabled = true
port = 9002
allowed_ips = ["*", "127.0.0.1"]
[policy_engine.config_mode]
mode = "xds"
[policy_engine.xds]
connect_timeout = "10s"
request_timeout = "5s"
initial_reconnect_delay = "1s"
max_reconnect_delay = "60s"
[policy_engine.xds.tls]
enabled = false
[policy_engine.file_config]
path = ""
[policy_engine.logging]
level = "info"
format = "text"
[policy_engine.metrics]
enabled = true
port = 9003

[analytics]
enabled = false
allow_payloads = false
enabled_publishers = []

[policy_configurations.llm_cost_v1]
pricing_file = "/etc/policy-engine/llm-pricing/model_prices.json"

[immutable_gateway]
enabled = false
artifacts_dir = "/etc/api-platform-gateway/immutable_gateway/artifacts"

[tracing]
enabled = false
endpoint = "otel-collector:4317"
insecure = true
service_version = "1.0.0"
batch_timeout = "1s"
max_export_batch_size = 512
sampling_rate = 1.0
